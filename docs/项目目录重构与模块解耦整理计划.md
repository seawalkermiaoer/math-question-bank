# 本地化数学题库系统：项目目录重构与模块解耦整理计划

> **文档状态**：目录整理阶段已实施  
> **创建时间**：2026-08-08  
> **最近更新**：2026-08-08  
> **适用范围**：后端架构解耦、根目录精简、Git 演进与跨平台 Release 兼容

## 一、实施原则

本次整理采用“入口稳定、业务下沉、工具归类、数据不迁移”的低风险策略：

1. 根目录 `main.py` 继续作为 `uvicorn main:app` 入口，双击启动方式保持不变。
2. 正在使用的 `math_question_bank.db` 继续保留在根目录，避免旧版升级与 Release 数据迁移风险。
3. 后端领域模块统一收敛到已有的 `mathbank/`，不再新建重复的 `core/` 包。
4. 运维、迁移、检索和打包工具统一收敛到 `scripts/`。
5. 所有持久化与资源路径继续由 `mathbank.paths` 锚定 `PROJECT_ROOT`。
6. 历史数据库和测试产物只归档、不删除。

## 二、实施后的目录结构

```text
题库项目/
├── 启动题库系统.command / .bat        # 根目录保留：桌面双击启动器
├── main.py                          # 根目录保留：FastAPI 入口
├── requirements.txt
├── requirements-dev.txt
├── .env / .env.example
├── math_question_bank.db            # 根目录保留：主数据库
├── README.md / AGENTS.md
├── LICENSE
│
├── mathbank/                        # 后端业务领域包
│   ├── __init__.py
│   ├── paths.py
│   ├── database.py
│   ├── metadata.py
│   ├── prompts.py
│   ├── ai_providers.py
│   ├── ai_http.py
│   ├── ai_json.py
│   ├── paper_helper.py
│   └── sync_helper.py
│
├── scripts/                         # 运维、迁移与 CLI 工具
│   ├── __init__.py
│   ├── search_questions.py
│   ├── migrate_fillin.py
│   ├── migrate_choice_parentheses.py
│   └── build_release.py
│
├── docs/
│   ├── AI_DATABASE_GUIDE.md
│   ├── 项目目录重构与模块解耦整理计划.md
│   └── images/
│       ├── screenshot1.png
│       └── screenshot2.png
│
├── data_backup/
│   ├── archive/                     # 历史迁移/失败数据库归档
│   ├── questions_backup.json
│   └── questions_library.md
│
├── tests/
│   └── artifacts/                   # 排版测试图片归档
├── static/
└── templates/
```

## 三、已完成任务

- [x] 将 `database.py`、`paper_helper.py`、`sync_helper.py` 移入 `mathbank/`。
- [x] 将所有后端、测试和工具脚本导入改为 `mathbank.*`。
- [x] 将检索、迁移和 Release 工具移入 `scripts/`，增加 `scripts/__init__.py`。
- [x] 将 CLI 命令统一为 `python3 -m scripts.<模块名>`。
- [x] 更新 Release 打包清单，完整复制 `mathbank/` 与 `scripts/`。
- [x] 将 README 图片移入 `docs/images/` 并修正引用。
- [x] 将 AI 检索指南和本计划移入 `docs/`。
- [x] 将历史数据库移入 `data_backup/archive/`，主数据库保持不变。
- [x] 将排版测试图移入 `tests/artifacts/`。
- [x] 同步更新 README、AGENTS 与 CLAUDE。

## 四、标准命令

从项目根目录运行：

```bash
# 启动服务
python3 -m uvicorn main:app --reload

# 检索题库
python3 -m scripts.search_questions -q "导数"

# 数据迁移
python3 -m scripts.migrate_fillin
python3 -m scripts.migrate_choice_parentheses

# 构建 Release
python3 -m scripts.build_release

# 自动化测试
PYTHONPATH=. pytest -q
```

## 五、兼容性与后续方向

- `main.py`、启动器和主数据库位置不变，因此日常使用方式不变。
- Python 文件换目录只改变源码组织，不会增加可感知的运行负担。
- Git 可依据内容相似度识别文件重命名，历史可通过 `git log --follow mathbank/paper_helper.py` 查询。
- Release 用户仍可通过复制旧版 `.env` 与 `math_question_bank.db` 完成升级。
- 后续若继续缩减 `main.py`，应优先按业务域建立 `mathbank/routes/` 与 `mathbank/services/`，并逐模块提交和回归测试，不再进行大规模一次性迁移。
